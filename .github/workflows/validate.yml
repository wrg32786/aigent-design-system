name: Validate design system
on:
  pull_request:
  push:
    branches: [master]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run registry
      - run: npm run intelligence
      - run: npm run inspiration
      - run: npm run resolve:check
      - run: npm run vision:check
      - run: npm run studio:check
      - run: npm run eval
      - name: Exercise the local CLI
        run: |
          rm -rf /tmp/aigent-design-install /tmp/aigent-design-plan.json /tmp/aigent-inspiration /tmp/aigent-inspiration-plan.json
          node scripts/cli.mjs doctor
          node scripts/cli.mjs add full-studio --target /tmp/aigent-design-install
          test -f /tmp/aigent-design-install/.claude/skills/aigent-design/SKILL.md
          test -f /tmp/aigent-design-install/.claude/skills/design-resolver/SKILL.md
          test -f /tmp/aigent-design-install/.claude/skills/visual-design-critic/SKILL.md
          test -f /tmp/aigent-design-install/.claude/skills/aigent-studio/SKILL.md
          test -f /tmp/aigent-design-install/inspiration/README.md
          test -f /tmp/aigent-design-install/resolve/README.md
          test -f /tmp/aigent-design-install/vision/README.md
          test -f /tmp/aigent-design-install/studio/index.html
          test -f /tmp/aigent-design-install/scripts/studio-server.mjs
          test -f /tmp/aigent-design-install/scripts/vision-review.mjs
          node scripts/cli.mjs plan design-intelligence/example-brief.json --out /tmp/aigent-design-plan.json
          node scripts/cli.mjs inspire add inspiration/examples/editorial-reference.json --root /tmp/aigent-inspiration --id editorial --kind structured-reference --analysis inspiration/examples/editorial-reference.json
          node scripts/cli.mjs inspire add inspiration/examples/immersive-reference.json --root /tmp/aigent-inspiration --id immersive --kind structured-reference --analysis inspiration/examples/immersive-reference.json
          node scripts/cli.mjs inspire add inspiration/examples/interface-reference.json --root /tmp/aigent-inspiration --id interface --kind structured-reference --analysis inspiration/examples/interface-reference.json
          node scripts/cli.mjs inspire compose --root /tmp/aigent-inspiration --brief inspiration/evals/briefs/immersive-product-launch.json --refs editorial,immersive,interface --out /tmp/aigent-inspiration-plan.json
          test -s /tmp/aigent-design-plan.json
          test -s /tmp/aigent-inspiration-plan.json
      - name: Validate the GitHub registry with shadcn
        run: |
          REF="${GITHUB_HEAD_REF:-$GITHUB_SHA}"
          npx --yes shadcn@latest registry validate "${GITHUB_REPOSITORY}#${REF}"
  browser:
    needs: check
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install chromium
      - run: npm run studio:check -- --browser
      - name: Start preview server
        run: |
          npm run serve > /tmp/aigent-design-system-server.log 2>&1 &
          echo $! > /tmp/aigent-design-system-server.pid
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4177/ > /dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/aigent-design-system-server.log
          exit 1
      - run: npm run smoke
      - run: npm run inspiration:smoke
      - name: Exercise Resolve and prepare visual review
        run: |
          rm -rf artifacts/resolve
          node scripts/cli.mjs resolve \
            --target templates/modular-scroll-starter \
            --url http://127.0.0.1:4177/templates/modular-scroll-starter/ \
            --out artifacts/resolve \
            --no-fail
          cat artifacts/resolve/latest.md
          test -s artifacts/resolve/latest.json
          node -e 'const r=require("./artifacts/resolve/latest.json"); if(!r.gate.pass||r.browserEvidence.length!==3||r.captures.length!==4)process.exit(1)'
          node scripts/cli.mjs vision prepare \
            --target templates/modular-scroll-starter \
            --url http://127.0.0.1:4177/templates/modular-scroll-starter/ \
            --out artifacts/resolve
          test -s artifacts/resolve/latest.visual-review-task.json
          test -s artifacts/resolve/latest.visual-review.template.json
          test -s artifacts/resolve/latest.visual-review.prompt.md
          node -e 'const t=require("./artifacts/resolve/latest.visual-review-task.json"); if(t.requiredViewports.length!==4||t.captures.length!==4)process.exit(1)'
          RUN_ID=$(node -p 'require("./artifacts/resolve/latest.json").runId')
          test -s artifacts/resolve/runs/$RUN_ID/element-map.json
          test -s artifacts/resolve/runs/$RUN_ID/desktop.annotated.png
          test -s artifacts/resolve/runs/$RUN_ID/tablet.annotated.png
          test -s artifacts/resolve/runs/$RUN_ID/mobile.annotated.png
          test -s artifacts/resolve/runs/$RUN_ID/reduced-motion.annotated.png
      - run: npm run capture
      - uses: actions/upload-artifact@v4
        with:
          name: visual-proof
          path: |
            artifacts/visual
            artifacts/resolve
            artifacts/studio
          if-no-files-found: error
          retention-days: 14
      - name: Stop preview server
        if: always()
        run: kill "$(cat /tmp/aigent-design-system-server.pid)" 2>/dev/null || true
  release:
    needs: [check, browser]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Publish validated package version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(node -p 'require("./package.json").version')
          TAG="v${VERSION}"
          TARGET="$GITHUB_SHA"
          if [ "$TAG" = "v0.6.0" ]; then TARGET="6bc610939b1c8595bf695bbdcca9ef824e56f03b"; fi

          git fetch --tags --force
          if ! git rev-parse -q --verify "refs/tags/$TAG" >/dev/null; then
            git tag -a "$TAG" "$TARGET" -m "$TAG"
            git push origin "$TAG"
          fi

          awk -v version="$VERSION" '
            $0 ~ "^## " version " " { found=1 }
            found && $0 ~ "^## " && $0 !~ "^## " version " " { exit }
            found { print }
          ' CHANGELOG.md > /tmp/release-notes.md
          test -s /tmp/release-notes.md

          TITLE=$(head -1 /tmp/release-notes.md | sed -E 's/^## [^ ]+ — //')
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release edit "$TAG" --title "$TAG — $TITLE" --notes-file /tmp/release-notes.md --latest
          else
            gh release create "$TAG" --title "$TAG — $TITLE" --notes-file /tmp/release-notes.md --latest --verify-tag
          fi

          if [ "$TAG" = "v0.6.0" ]; then
            for branch in \
              docs/fix-readme-hero-headline \
              docs/hero-text-overlay \
              docs/readme-real-proof \
              docs/readme-visual-overhaul \
              docs/replace-readme-images \
              feature/aigent-studio \
              release/v0.6.0 \
              ci/validated-releases
            do
              encoded="${branch//\//%2F}"
              gh api --method DELETE "repos/${GITHUB_REPOSITORY}/git/refs/heads/${encoded}" || true
            done
          fi
